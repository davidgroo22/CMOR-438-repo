from setuptools import setup, find_packages

setup(
    name="ml_from_scratch",
    version="0.1.0",
    packages=find_packages(),
    install_requires=["numpy"],
    description="Machine learning algorithms implemented from scratch (Rice CMOR 438)",
)